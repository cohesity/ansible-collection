# Playbook to register Oracle source to the cluster.
#
# Standalone (default):
#   ansible-playbook register_oracle_source.yml -i ../ansible_config.ini
#
# RAC:
#   ansible-playbook register_oracle_source.yml -i ../ansible_config.ini \
#     -e "oracle_source_type=rac"
---
- name: Playbook to register Oracle source to the cluster
  hosts: localhost
  become: false

  vars:
    oracle_source_type: standalone

  tasks:
    - name: "Cohesity Protection Source: Set Source to state of present"
      cohesity.dataprotect.cohesity_oracle_source:
        cluster: "{{ cohesity_server }}"
        username: "{{ cohesity_username }}"
        password: "{{ cohesity_password }}"
        validate_certs: "{{ cohesity_validate_certs }}"
        state: present
        force_register: true
        source_type: "{{ oracle_source_type }}"
        endpoint: "{{ rac_endpoint | default('') if oracle_source_type == 'rac' else oracle_endpoint }}"
        scan_vip_address: "{{ scan_vip_address | default('') }}"
