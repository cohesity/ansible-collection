ancestor: 1.3.0
releases:
  1.4.2:
    changes:
      bugfixes:
      - Fixed LiteralPath Issue During Cohesity Window Agent Installation
      - Fixed collection failure issues for some ansible version
      major_changes:
      - Ansible-Core Minimum Version Bumped to 2.16
    release_date: '2025-01-30'
  1.4.3:
    changes:
      bugfixes:
      - cohesity_agent - Fixed agent detection failing with "Cohesity Agent is partially
        installed" on systemd hosts where the SysV init script is not created by the
        installer.
    release_date: '2026-04-22'
  1.4.4:
    changes:
      bugfixes:
      - ansible-lint - Removed ``name[template]`` from ``.ansible-lint`` skip_list
        and fixed task names across playbooks to ensure Jinja2 template variables
        appear only at the end of task name strings.
      - galaxy.yml - Added ``ansible.cfg`` and ``.ansible`` to ``build_ignore`` to
        prevent local developer configuration files from being packaged into the collection
        tarball.
      security_fixes:
      - Bumped ``requests`` minimum version to ``>=2.31.0`` to address known security
        vulnerabilities in older versions.
      minor_changes:
      - Added Changelog section to README with recent version highlights.
      - Added Support (Red Hat Users) section to README with Ansible Automation Hub
        guidance.
      - removed ansible-core from requirements.txt
    release_date: '2026-05-21'
  1.4.5:
    changes:
      bugfixes:
      - 'Added ``Content-Type: application/json`` header to REST requests across
        collection modules to prevent HTTP 415 errors.'
      minor_changes:
      - Oracle workflow playbooks (``register_oracle_source``, ``refresh_oracle_source``,
        ``create_oracle_job``, ``recover_db``) support standalone and RAC. Pass all
        settings with ``-i localhost,`` and ``-e`` (``ansible_connection=local``, cluster
        credentials, ``oracle_source_type``, ``scan_vip_address``, optional ``rac_endpoint``).
      - cohesity_oracle_job - Added ``source_type`` option for standalone and RAC
        protection jobs.
      - cohesity_oracle_restore - Added ``source_type`` option to recover standalone
        and RAC databases.
      - cohesity_oracle_source - Added support to register Oracle RAC clusters using
        ``scan_vip_address`` (SCAN/VIP address) and optional ``rac_endpoint`` when the
        cluster cannot reach the SCAN/VIP directly.
    fragments: []
    release_date: '2026-06-03'
